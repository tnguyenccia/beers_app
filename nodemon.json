{
    "watch": ["src"],
    "ext": "ts,js,json",
    "ignore": ["node_modules", "build", "converage"],
    "exec": "ts-node -r tsconfig-paths/register ./src/api/server.ts",
    "restartable": "rs",
    "env": {
        "NODE_ENV": "development"
    }
}